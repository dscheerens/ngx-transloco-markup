import { defineTranslationKeys } from '../../common/utils/define-translation-keys';

export const BASIC_FEATURE_TRANSLATION_KEYS = defineTranslationKeys((t) => ({
    BASIC: {
        TITLE: t,
        NAME: t,
        GREETING: t
    }
})).BASIC;
